version: '3.8'

services:
  cast-mcp-bridge:
    build: .
    container_name: cast-mcp-bridge
    restart: unless-stopped
    # Stále klíčové pro mDNS objevování Chromecastů v LAN
    network_mode: host
